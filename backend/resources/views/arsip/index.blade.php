@extends('layouts.app') @section('content')<div class="card"><h2>Arsip Dokumen</h2><form><input name="q" value="{{ $q }}" placeholder="Cari kode, nama atau kategori..."></form><br><a class="btn primary" href="{{ route('arsip.create') }}">+ Tambah Arsip</a></div><div class="card"><table><tr><th>Kode</th><th>Nama</th><th>Kategori</th><th>Tanggal</th><th>Lokasi</th><th>File</th><th>Aksi</th></tr>@forelse($data as $x)<tr><td>{{ $x->kode }}</td><td>{{ $x->nama }}</td><td>{{ $x->kategori }}</td><td>{{ $x->tanggal?->format('d/m/Y') }}</td><td>{{ $x->lokasi }}</td><td>@if($x->file_path)<a href="{{ asset('storage/'.$x->file_path) }}" target="_blank">Lihat</a>@endif</td><td><a href="{{ route('arsip.edit',$x) }}">Edit</a> <form style="display:inline" method="post" action="{{ route('arsip.destroy',$x) }}">@csrf @method('DELETE')<button class="btn danger">Hapus</button></form></td></tr>@empty<tr><td colspan="7">Belum ada data.</td></tr>@endforelse</table>{{ $data->links() }}</div>@endsection
