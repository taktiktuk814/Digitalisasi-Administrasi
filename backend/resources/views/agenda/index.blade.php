@extends('layouts.app') @section('content')<div class="card"><h2>Agenda Kegiatan</h2><form><input name="q" value="{{ $q }}" placeholder="Cari kegiatan atau tempat..."></form><br><a class="btn primary" href="{{ route('agenda.create') }}">+ Tambah Agenda</a></div><div class="card"><table><tr><th>Tanggal</th><th>Waktu</th><th>Kegiatan</th><th>Tempat</th><th>Penanggung Jawab</th><th>Aksi</th></tr>@forelse($data as $x)<tr><td>{{ $x->tanggal?->format('d/m/Y') }}</td><td>{{ $x->waktu }}</td><td>{{ $x->kegiatan }}</td><td>{{ $x->tempat }}</td><td>{{ $x->penanggung_jawab }}</td><td><a href="{{ route('agenda.edit',$x) }}">Edit</a> <form style="display:inline" method="post" action="{{ route('agenda.destroy',$x) }}">@csrf @method('DELETE')<button class="btn danger">Hapus</button></form></td></tr>@empty<tr><td colspan="6">Belum ada data.</td></tr>@endforelse</table>{{ $data->links() }}</div>@endsection
