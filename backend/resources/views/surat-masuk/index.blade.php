@extends('layouts.app') @section('content')<div class="card"><h2>Surat Masuk</h2><form><input name="q" value="{{ $q }}" placeholder="Cari nomor, asal atau perihal..."></form><br><a class="btn primary" href="{{ route('surat-masuk.create') }}">+ Tambah Surat</a></div><div class="card"><table><tr><th>Nomor</th><th>Tanggal</th><th>Asal</th><th>Perihal</th><th>Status</th><th>Aksi</th></tr>@forelse($data as $x)<tr><td>{{ $x->nomor }}</td><td>{{ $x->tanggal?->format('d/m/Y') }}</td><td>{{ $x->asal }}</td><td>{{ $x->perihal }}</td><td>{{ $x->status }}</td><td><a href="{{ route('surat-masuk.edit',$x) }}">Edit</a> <form style="display:inline" method="post" action="{{ route('surat-masuk.destroy',$x) }}">@csrf @method('DELETE')<button class="btn danger">Hapus</button></form></td></tr>@empty<tr><td colspan="6">Belum ada data.</td></tr>@endforelse</table>{{ $data->links() }}</div>@endsection
